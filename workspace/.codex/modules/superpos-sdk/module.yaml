description: "Superpos Python SDK — programmatic access to the Superpos API (tasks, knowledge, agents, service workers)"
env:
  - SUPERPOS_BASE_URL
  - SUPERPOS_HIVE_ID
  - SUPERPOS_AGENT_ID
  - SUPERPOS_API_TOKEN
  - SUPERPOS_REFRESH_TOKEN
deps:
  python: []
  system: []
